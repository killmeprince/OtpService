spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

spring.flyway.enabled=false

security.jwt.secret=TestJwtSecretTestJwtSecretTestJwtSecret
security.jwt.expirationMs=3600000

scheduler.expire-otp.cron=0 */5 * * * *

email.username=test@example.com
email.password=testpass

spring.mail.host=localhost
spring.mail.port=25
spring.mail.username=${email.username}
spring.mail.password=${email.password}
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

smpp.host=localhost
smpp.port=2775
smpp.system-id=dummySystem
smpp.password=dummyPass
smpp.source-addr=OTPService

sms.emulator.output-file=sms-emulator.log

telegram.bot-token=dummyToken123
telegram.chat-id=987654321
